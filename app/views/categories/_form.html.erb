<%= form_with(model: category, local: true) do |form| %>
  <% if category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(category.errors.count, "error") %> prohibited this category from being saved:</h2>

      <ul>
      <% category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.text_field :title, placeholder: "Give your category a title" %>
  </div>

  <div class="field">
    
    <%= form.text_field :color, class: "colorwheel", placeholder: "Add a color" %>
  </div>

  <div class="actions">
    <%= form.submit "Submit Category", class: "button success large expanded" %>
  </div>
<% end %>
